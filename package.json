{
  "name": "swaglabs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
      "test": "playwright test",
      "test:deploy": "npx allure-commandline generate allure-results -o allure-report --clean && npx netlify-cli deploy --dir=allure-report --prod --json > deploy-output.json",
      "test:email": "npx ts-node send-report.ts",
      "test:run-and-report": "npm run test && npm run test:deploy && npm run test:email"
    },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/archiver": "^7.0.0",
    "@types/node": "^24.9.1",
    "@types/nodemailer": "^7.0.3",
    "allure-commandline": "^2.34.1",
    "allure-playwright": "^3.4.2"
  },
  "dependencies": {
    "archiver": "^7.0.1",
    "dotenv": "^17.2.3",
    "googleapis": "^164.1.0",
    "nodemailer": "^7.0.10"
  }
}
